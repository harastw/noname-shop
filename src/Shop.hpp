#pragma once

#include <Product.h>
#include <Apple.h>

class Shop
{
public:
  Shop(std::list<std::pair<Product,int> init_list) {
    for (auto it = init_list.begin(); it != init_list.end(); it++) {
      for (auto it2 = 
      counter.push_back(init_list.first);
    }
  }
private:
  bag counter;
};
